# 🎬 Ленивая загрузка видео в AR Lab Popup-ах

## Проблема
Все видео внутри popup-ов загружались сразу при загрузке страницы, что сильно замедляло работу.

## Решение
Реализована ленивая загрузка видео:
- ✅ Видео загружаются **только при открытии popup-а**
- ✅ Загружается **только видео в активном слайде** свайпера
- ✅ При переключении слайдов загружаются новые видео
- ✅ Видео в неактивных слайдах ставятся на паузу для экономии ресурсов

---

## 🔧 Как это работает

### 1. Изменения в разметке Webflow

Нужно вручную изменить атрибуты у всех видео внутри `[ar-lab_popup]`:

#### Было:
```html
<source src="https://example.com/video.mp4" type="video/mp4">
```

#### Стало:
```html
<source lazy-src="https://example.com/video.mp4" type="video/mp4">
```

#### Для Webflow Background Video:

**Было:**
```html
<div data-video-urls="https://example.com/video.mp4" data-poster-url="...">
```

**Стало:**
```html
<div lazy-data-video-urls="https://example.com/video.mp4" data-poster-url="...">
```

### 2. Утилита (уже готова)

Файл: `src/utils/popupVideoLazyLoad.ts`

**Что делает:**
1. Отслеживает открытие popup-а (`data-popup-visible="true"`)
2. Находит активный слайд (`.swiper-slide-active`)
3. Переименовывает `lazy-src` → `src` и `lazy-data-video-urls` → `data-video-urls`
4. Запускает `video.load()` и `video.play()`
5. Отслеживает переключение слайдов через `MutationObserver`
6. Активирует видео в новом активном слайде
7. Ставит на паузу видео в предыдущих слайдах

---

## 📋 Пошаговая инструкция

### Шаг 1: Измените атрибуты в Webflow

Для каждого popup-а с видео:

1. Откройте Custom Code видео-элемента
2. Найдите все `src=` и замените на `lazy-src=`
3. Найдите `data-video-urls=` и замените на `lazy-data-video-urls=`
4. Сохраните изменения

**Пример полной разметки:**

```html
<div class="swiper-slide is-ar-lab_popup">
  <div class="popup-video-new w-embed">
    <div 
      data-poster-url="https://example.com/poster.jpg" 
      lazy-data-video-urls="https://example.com/video.mp4" 
      data-autoplay="true" 
      data-loop="true" 
      class="abs-image w-background-video w-background-video-atom"
    >
      <video id="bg-video-01" autoplay loop muted playsinline>
        <source lazy-src="https://example.com/video.mp4" type="video/mp4">
      </video>
    </div>
  </div>
</div>
```

### Шаг 2: Опубликуйте Webflow сайт

После изменения всех атрибутов опубликуйте сайт.

### Шаг 3: Обновите JavaScript бандл

JavaScript утилита уже подключена в `src/index.ts`. Просто:

```bash
pnpm run build
```

И загрузите обновленный `dist/index.js` на сайт.

---

## ✅ Как проверить что работает

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Network**
3. Отфильтруйте по **Media** или **file.mp4**
4. Обновите страницу
5. **Ожидаемый результат:** Видео НЕ загружаются

6. Откройте любой popup
7. **Ожидаемый результат:** 
   - Загружается только 1 видео (из активного слайда)
   - Видео автоматически начинает проигрываться

8. Переключите слайд в свайпере
9. **Ожидаемый результат:**
   - Загружается видео из нового слайда
   - Предыдущее видео ставится на паузу

---

## 🎯 Преимущества решения

### ✅ Простота
- Не требует сложных inline скриптов
- Вся логика в одной утилите

### ✅ Производительность
- Видео загружаются только при необходимости
- Только активные видео проигрываются
- Экономия трафика и памяти

### ✅ Масштабируемость
- Автоматически работает для любого количества popup-ов
- Автоматически работает для любого количества слайдов
- Не требует дополнительных настроек

### ✅ UX
- Видео мгновенно начинают играть при открытии
- Нет задержек из-за одновременной загрузки всех видео
- Плавное переключение между слайдами

---

## 🐛 Отладка

### Если видео все еще загружаются сразу

**Проверьте:**
1. Все ли атрибуты `src` заменены на `lazy-src`?
2. Все ли `data-video-urls` заменены на `lazy-data-video-urls`?
3. Загружен ли обновленный `dist/index.js`?
4. Очищен ли кэш браузера?

### Если видео не загружаются при открытии popup-а

**Проверьте в консоли:**
```javascript
// Должна быть доступна функция
window.Webflow
```

**Проверьте в Elements:**
- Popup должен иметь `data-popup-visible="true"` когда открыт
- Активный слайд должен иметь класс `.swiper-slide-active`

---

## 📁 Файлы

- **Утилита:** `src/utils/popupVideoLazyLoad.ts`
- **Подключение:** `src/index.ts` (строка 13, 25)
- **Собранный бандл:** `dist/index.js`

---

## 🚀 Готово!

После выполнения всех шагов:
- Страница загружается быстро (без видео)
- Видео загружаются только при открытии popup-а
- Только активные слайды загружают видео
- Экономия трафика и ресурсов

Если возникнут вопросы - проверьте консоль браузера на ошибки.

